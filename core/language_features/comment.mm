#!/usr/bin/env mammal -v aardvark
"""
# A package defining comments

This package does not include the syntax for compiler hints,
which look rather similar but are different.
"""

comment := __operators__.add(" # ", __environments__["comment"])
